# Server Configuration
PORT=5000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://username:password@localhost:5432/event_management"

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
TICKET_DOWNLOAD_SECRET=your_ticket_download_signing_secret
TEAM_INVITE_SECRET=your_team_invite_signing_secret
CHECKOUT_ACCESS_SECRET=your_checkout_access_signing_secret

# AWS S3 Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
S3_BUCKET_NAME=event-management-bucket

# Payment Gateway - Razorpay
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

# Payment Gateway - PhonePe
# Get these from PhonePe Developer Dashboard: https://developer.phonepe.com
# Sandbox also requires explicit credentials; the app does not ship hardcoded test keys.
PHONEPE_CLIENT_ID=your_client_id
PHONEPE_CLIENT_SECRET=your_client_secret
PHONEPE_CLIENT_VERSION=1
PHONEPE_ENV=sandbox

# Optional legacy PhonePe V1 credentials, only if not using Standard Checkout V2
PHONEPE_MERCHANT_ID=your_legacy_merchant_id
PHONEPE_SALT_KEY=your_legacy_salt_key
PHONEPE_SALT_INDEX=1

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
EMAIL_FROM=noreply@eventmanagement.com
SMTP_FROM_EMAIL=noreply@eventmanagement.com

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# QR Code Secret
# Required in production. Development uses a local-only fallback if omitted.
QR_SECRET_KEY=your_qr_code_signing_secret
ALLOW_UNSIGNED_TICKET_SCAN=false

# Frontend URL
FRONTEND_URL=http://localhost:5173
CORS_ORIGINS=http://localhost:5173
TRUST_PROXY=false
APP_VERSION=local

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# Optional: public DNS fallback timeout for Neon pooled hosts
NEON_DNS_FALLBACK_TIMEOUT_MS=2500

# Optional maintenance scripts
# ADMIN_EMAIL=user@example.com
# CONFIRM_ADMIN_EMAIL=user@example.com
# SMOKE_BASE_URL=http://localhost:5000
# ALLOW_DEBUG_SCRIPTS=false
# DEBUG_SCAN_BASE_URL=http://localhost:5000
# DEBUG_SCAN_TICKET_PREFIX=abc123
# DEBUG_SCAN_EMAIL=user@example.com
# DEBUG_SCAN_PASSWORD=your_password
# DEBUG_EXPIRY_EVENT_QUERY=event title fragment
# DEBUG_TICKET_VERIFY=false
# CLOUDINARY_DEBUG_PUBLIC_ID=raw/public-id.pdf
# CLOUDINARY_DEBUG_BACKUP_ASSET_ID=optional_asset_id
# CLOUDINARY_DEBUG_URL=https://res.cloudinary.com/...

# Cloudinary (for production image storage)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Cloudflare R2 (primary storage for generated PDF assets; Cloudinary remains fallback)
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret_key
R2_BUCKET=your_r2_bucket_name
R2_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com
R2_REGION=auto

# Twilio (for SMS/WhatsApp notifications)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Push Notifications (generate with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# Wallet integrations
APPLE_PASS_TYPE_ID=pass.com.example.occasio
APPLE_TEAM_ID=your_apple_team_id
GOOGLE_WALLET_ISSUER_ID=your_google_wallet_issuer_id
